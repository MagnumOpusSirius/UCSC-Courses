C++ is a compiled language. 

2 Steps:

Compilation: Human readable cpp code and convert into something computer can understand. 

Linking: Link the executable/application that runs the compiled code

OOP:
Structure our code around classes.
Class has name, email and verified.

Example of class:
this instance or objects is: 
Parth, pmahale@ucsc.edu and verified=true

C++ used for game engine, video editing software, or modeling program, so computationally heavy. Better in performance than Java or C# etc.

BASICC:
    //std is namespace
    //cout is an instance/object from ostream (class)
    //<< is an operator 
    //preface cout object in std namespace